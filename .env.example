# Example environment variables for local development.
# Copy this file to `.env` and fill in values as needed.
#
# IMPORTANT:
# - Do NOT commit `.env` (it is gitignored).
# - The GitHub MCP server (github/github-mcp-server) requires a token to start.
#
# Fine-grained PAT: grant access to the target repo (and org if needed)
# Classic PAT: `repo` (and optionally `read:org`) is typically sufficient.
GITHUB_PERSONAL_ACCESS_TOKEN=
