# 🧾 INSTALL LOG — May 13, 2025

Executed via `dev_bootstrap.sh`

### Installed via Homebrew
- zsh, fzf, bat, eza, tree, fd, ripgrep, htop
- jq, yq, glow, git, gh, rsync, zoxide
- pyenv, pipx, poetry, pre-commit, tmux
- direnv, watch, nvm, imagemagick, ffmpeg, sox

### Python Config
- `pyenv` for version control
- `pipx` isolated CLI tooling
- `poetry` for environment and dependency management

### Shell Enhancements
- `oh-my-zsh` installed
- `.zshrc_template` configured
- Aliases defined in `terminal-aliases.md`