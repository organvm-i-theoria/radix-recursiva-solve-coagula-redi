# 📘 .zshrc_template Version Registry
📁 Folder: TM-TP_00-01_terminal-ops  
🗂️ File: `.zshrc_template`  
UID: ZSHRC_TMPL_001

---

## 🔖 Version History

### v1a — 2025-05-13
- ✅ Initial version for RE:GE_OS bootstrapping
- ✅ Welcome banner on shell load (`echo`)
- ✅ `zoxide`, `direnv`, `pyenv` hooks initialized
- ✅ Core aliases (`ll`, `la`, `gs`, `gd`, `pycheck`)
- ✅ `oh-my-zsh` source fix (quote error resolved)
- ✅ Conditional `poetry shell` when `pyproject.toml` is present
- ✅ Finalized version added to Vault with UID + folder_map and registry references

---

## 🔄 Planned Additions
- [ ] Auto-activate `.venv` if no `poetry.lock` present
- [ ] Conditional `conda`/`micromamba` support (if desired)
- [ ] Visual theming toggle via flag (`export THEME_MODE=light|dark`)
- [ ] Add `vaultmap` terminal alias to print folder structure on demand